import 'package:flutter/material.dart';

class ImportAccountView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Import Account')),
      body: Text('Import Account'),
    );
  }
}
